# Disabled on freifunk/voctoweb — upstream pushes images to ghcr.io/voc/voctoweb.
# Freifunk builds production images on the app host (Dockerfile.production).
# Keep this file so upstream sync conflicts are obvious; resolve by keeping ours.
name: Container Image (disabled)

on:
  workflow_dispatch:

jobs:
  build:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "VOC container image publish is disabled on freifunk/voctoweb"

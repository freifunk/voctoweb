# No-op on freifunk/voctoweb (Dependabot auto-merge is VOC-only).
# Renovate handles dependency PRs here. Keep file for upstream sync clarity.
name: Dependabot auto-merge (disabled)

on:
  workflow_dispatch:

jobs:
  auto-merge:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dependabot auto-merge is disabled on freifunk/voctoweb"
